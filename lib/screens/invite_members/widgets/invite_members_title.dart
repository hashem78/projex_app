import 'package:flutter/material.dart';

class InviteMembersScreenTitle extends StatelessWidget {
  const InviteMembersScreenTitle({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Invite new members",
    );
  }
}
